This directory allows you to inject additional JSON recipes into the game. They
don't have to be Smithy recipes.

This is basically a datapack where every file is already in a
"data/smithy_custom/recipes" directory.
